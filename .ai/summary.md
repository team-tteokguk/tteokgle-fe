## 🤖 AI Change Summary for PR #7

### Chunk 1/1
### 개요
이번 변경 사항은 커밋 메시지 포맷을 수정하고, 회원 관련 API 및 훅을 추가하여 사용자 프로필 관리 기능을 구현한 것입니다.

### 주요 변경 사항
1. **커밋 메시지 포맷 수정**:
   - `.husky/prepare-commit-msg` 파일에서 커밋 메시지에 이슈 번호를 추가하는 방식이 변경되었습니다. 기존에는 메시지의 시작 부분에 이슈 번호를 추가하였으나, 이제는 메시지의 끝에 추가하도록 수정되었습니다.

2. **회원 API 추가**:
   - `src/features/members/api/memberApi.ts` 파일에 사용자 프로필 조회, 닉네임 업데이트, 회원 탈퇴 기능을 제공하는 API 함수가 추가되었습니다.

3. **회원 키 추가**:
   - `src/features/members/api/memberKeys.ts` 파일에 회원 관련 쿼리 키를 정의하는 객체가 추가되었습니다.

4. **회원 훅 추가**:
   - `src/features/members/hooks/useMember.ts` 파일에 사용자 프로필 조회, 닉네임 업데이트, 회원 탈퇴를 위한 React Query 훅이 추가되었습니다.

5. **회원 타입 정의**:
   - `src/features/members/types/index.ts` 파일에 회원 정보를 나타내는 `Member` 인터페이스와 닉네임 업데이트를 위한 `MemberUpdateDto` 인터페이스가 추가되었습니다.

### 위험/영향
- 커밋 메시지 포맷 변경으로 인해 기존의 자동화된 커밋 메시지 처리 로직에 영향을 줄 수 있습니다. 이 부분은 테스트를 통해 확인이 필요합니다.
- 새로운 API와 훅이 추가됨에 따라, 이들을 사용하는 컴포넌트에서의 오류 처리 및 상태 관리가 필요합니다. 기존 코드와의 통합 테스트가 요구됩니다.

### 테스트/검증
- 커밋 메시지 포맷 변경에 대한 검증: 커밋 메시지를 작성할 때 이슈 번호가 올바르게 추가되는지 확인해야 합니다.
- 추가된 API와 훅에 대한 단위 테스트 및 통합 테스트를 작성하여 기능이 정상적으로 작동하는지 확인해야 합니다.

### 후속 조치
- 커밋 메시지 포맷 변경에 따른 기존 자동화 스크립트의 수정 여부를 검토해야 합니다.
- 추가된 API와 훅을 사용하는 컴포넌트에서의 오류 처리 및 사용자 피드백을 개선하기 위한 작업이 필요합니다.
- 회원 탈퇴 후의 사용자 경험(예: 토큰 삭제 및 로그인 페이지 이동 처리)을 구현해야 합니다.
