## 🤖 AI Change Summary for PR #4

### 개요
이번 패치는 `package-lock.json` 및 `package.json` 파일의 수정과 함께 새로운 의존성인 `axios`를 추가하고, Axios를 활용한 API 호출을 위한 새로운 서비스 파일을 생성하는 내용을 포함합니다. 이를 통해 프로젝트의 의존성 관리가 업데이트되며, API 통신의 일관성을 높이고 에러 처리를 중앙 집중화할 수 있습니다.

### 주요 변경 사항
1. **의존성 추가**:
   - `package.json`에 `axios` 패키지 버전 `^1.13.2`가 추가되었습니다.
   - `axios`와 관련된 여러 종속성(`follow-redirects`, `form-data`, `asynckit` 등) 및 여러 새로운 패키지(`call-bind-apply-helpers`, `combined-stream`, `delayed-stream`, `dunder-proto`, `es-errors`, `es-object-atoms`, `es-set-tostringtag`, `gopd`, `has-symbols`, `has-tostringtag`, `hasown`, `math-intrinsics` 등)가 추가되었습니다.

2. **새로운 서비스 파일 생성**:
   - `src/services/axios.ts` 파일이 추가되어 Axios 인스턴스를 설정하고, 요청 및 응답 인터셉터를 정의함.
   - 기본 URL은 환경 변수 `VITE_API_URL`을 사용하며, 없을 경우 `http://localhost:8080`으로 설정.
   - 요청 및 응답에 대한 에러 처리 로직이 추가되었습니다.

### 위험/영향
- 새로운 패키지 추가로 인해 의존성 충돌 가능성이 존재하며, 기존 코드와의 호환성 문제가 발생할 수 있습니다.
- Axios를 통한 API 호출 방식으로 변경됨에 따라, 기존 API 호출 방식과의 차이로 인한 버그 발생 가능성이 있습니다.
- 중앙 집중식 에러 처리 로직이 추가되어, 인증 관련 에러 처리 시 기존 로직과의 충돌 가능성이 존재합니다.

### 테스트/검증
- 새로운 의존성이 추가된 후, 전체 테스트 스위트를 실행하여 기존 기능이 정상적으로 작동하는지 확인해야 합니다.
- `axios` 라이브러리의 설치 및 정상 작동 여부를 확인하고, 새로운 Axios 인스턴스의 요청 및 응답 인터셉터 기능이 의도한 대로 작동하는지 테스트해야 합니다.
- API 호출 시 에러 처리 로직이 정상적으로 작동하는지 검증해야 합니다.

### 후속 조치
- API 호출에 대한 단위 테스트 및 통합 테스트를 작성해야 합니다.
- 인증 토큰 로직 추가 및 관련 기능 구현이 필요합니다.
- 기존 API 호출 방식과의 비교 및 성능 테스트를 수행해야 하며, 관련 문서 및 README 파일을 업데이트하여 새로운 패키지 사용법을 명시해야 합니다.
