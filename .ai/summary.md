## 🤖 AI Change Summary for PR #5

### Chunk 1/1
### 개요
이번 변경 사항은 코드 스타일 설정, ESLint 구성, 패키지 의존성 추가, 라우팅 및 인증 관련 컴포넌트 추가를 포함합니다. 주요 변경 사항은 Prettier 설정에서 세미콜론 사용을 활성화하고, 새로운 라우팅 구조를 도입하여 인증된 사용자만 접근할 수 있는 보호된 경로를 설정하는 것입니다.

### 주요 변경 사항
1. **Prettier 설정 변경**: `.prettierrc` 파일에서 `semi` 옵션을 `true`로 변경.
2. **ESLint 구성 수정**: `eslint.config.js` 파일에서 import 순서를 재정렬하고, `perfectionist` 플러그인 관련 규칙을 추가 및 수정.
3. **패키지 의존성 추가**: `package.json` 및 `package-lock.json` 파일에 `react-router`와 `zustand` 패키지를 추가.
4. **라우팅 및 인증 컴포넌트 추가**:
   - `ProtectedRoute` 컴포넌트 추가: 인증된 사용자만 접근 가능하도록 설정.
   - `GeneralLayout` 컴포넌트 추가: 공통 레이아웃을 정의.
   - `Main` 페이지 추가: 메인 페이지의 기본 구조를 정의.
   - `router.tsx` 파일 추가: 새로운 라우팅 구조를 설정.

### 위험/영향
- **코드 스타일**: Prettier 설정 변경으로 인해 기존 코드와의 일관성이 떨어질 수 있으므로, 팀원 간의 코드 스타일 합의가 필요.
- **라우팅 변경**: 새로운 라우팅 구조 도입으로 인해 기존 페이지 접근 방식에 변화가 생길 수 있으며, 인증 로직이 추가됨에 따라 사용자 경험에 영향을 줄 수 있음.
- **의존성 추가**: 새로운 패키지 추가로 인해 빌드 및 배포 과정에서의 의존성 관리가 필요.

### 테스트/검증
- Prettier 설정이 올바르게 적용되었는지 확인하기 위해 코드 포맷팅 테스트를 실행.
- ESLint 규칙이 제대로 작동하는지 확인하기 위해 코드 린팅을 수행.
- 새로운 라우팅 및 인증 로직이 정상적으로 작동하는지 확인하기 위해 유닛 테스트 및 통합 테스트를 작성하고 실행.

### 후속 조치
- 팀원들과의 코드 스타일 및 ESLint 규칙에 대한 논의 필요.
- 추가적인 페이지 및 컴포넌트 개발 시, 새로운 라우팅 구조에 맞춰 구현해야 함.
- 사용자 인증 및 상태 관리 로직에 대한 문서화를 진행하여 팀 내 공유.
