## 🤖 AI Change Summary for PR #16

### Chunk 1/1
## 개요
이번 변경 사항은 방명록 API의 경로 수정과 새로운 인증 관련 API 및 훅을 추가하여 사용자 인증 및 알림 기능을 개선한 내용입니다.

## 주요 변경 사항
1. **방명록 API 경로 수정**:
   - `useGuestBook.ts` 파일에서 `guestbookApi`의 경로를 `guestBookApi`로 대소문자 일치시킴.

2. **인증 API 추가**:
   - `authApi.ts` 파일에 로그아웃 및 리프레시 토큰 기능을 포함한 API 추가.
   
3. **인증 훅 추가**:
   - `useAuth.ts` 파일에 사용자 인증 상태를 관리하고 로그아웃 기능을 제공하는 훅 추가.

4. **알림 API 추가**:
   - `notificationApi.ts` 파일에 알림 목록 조회 및 일괄 읽음 처리 기능 추가.

5. **알림 훅 및 타입 추가**:
   - 알림 관련 훅과 타입 정의를 추가하여 알림 기능을 지원.

6. **Axios 인스턴스 추가**:
   - `axiosInstance.ts` 파일에 Axios 인스턴스를 생성하고 요청 및 응답 인터셉터를 설정하여 인증 토큰을 관리.

7. **상태 관리 수정**:
   - `useAuthStore.ts` 파일에서 인증 상태 및 액세스 토큰 관리 기능을 추가 및 수정.

## 위험/영향
- 방명록 API의 경로 대소문자 변경으로 인해 해당 API를 호출하는 부분에서 오류가 발생할 수 있으며, 이를 검토해야 함.
- 새로운 인증 및 알림 기능 추가로 인해 기존 코드와의 호환성 문제가 발생할 수 있으므로 충분한 테스트가 필요함.

## 테스트/검증
- 각 API와 훅의 기능이 정상적으로 작동하는지 확인하기 위해 단위 테스트 및 통합 테스트를 수행해야 함.
- 방명록, 인증, 알림 기능에 대한 테스트 케이스를 작성하고 실행해야 함.

## 후속 조치
- 변경된 API 경로가 다른 파일에서 사용되고 있는지 확인하고, 필요한 경우 추가적인 수정 작업을 진행해야 함.
- 추가된 API와 훅에 대한 문서화를 진행하여 개발자들이 쉽게 이해하고 사용할 수 있도록 해야 함.
