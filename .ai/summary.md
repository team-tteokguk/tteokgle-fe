## 🤖 AI Change Summary for PR #31

### 개요
이번 패치는 인증된 사용자 라우팅 수정, 방명록 API 응답 형식 개선, 상점 관련 API 및 UI 컴포넌트 추가, 판매 수량 조정 기능, Axios 인스턴스 수정 등 다양한 기능 개선을 포함하고 있습니다. 전체적으로 코드의 일관성과 안정성을 높이고 사용자 경험을 향상시키는 데 중점을 두었습니다.

### 주요 변경 사항
1. **라우팅 수정**:
   - 로그인 경로를 `/auth/signin`에서 `/login`으로 변경.
   - 새로운 상점 페이지 `/my-store` 추가.

2. **API 경로 및 응답 형식 수정**:
   - 방명록 API 경로를 `/store`에서 `/stores`로 변경.
   - `createItem` 함수의 API 경로를 수정.
   - 응답 데이터 형식을 정규화하는 `normalizeGuestBookList` 함수 추가.

3. **새로운 훅 및 컴포넌트 추가**:
   - `useGetMyStore`, `useGetMyStoreItems`, `CreateNewItemModal`, `GuestBookModal`, `ProductItem` 컴포넌트 추가.
   - 판매 수량 조정 UI 요소 추가 및 방명록 관리 기능 구현.

4. **Axios 인스턴스 수정**:
   - 토큰 갱신 요청 관리 및 401 에러 처리 로직 개선.

5. **유틸리티 함수 및 타입 정의 수정**:
   - `getAllGomyeongInfos` 함수 추가 및 여러 인터페이스 필드 수정.

### 위험/영향
- 라우팅 및 API 응답 형식 변경으로 인해 기존 인증 흐름 및 클라이언트 코드에서 오류가 발생할 수 있음.
- 새로운 기능 추가로 인해 사용자 경험이 향상될 것으로 기대되지만, 기존 코드와의 충돌 가능성도 존재.
- Axios의 응답 처리 로직 변경으로 인증 관련 버그 발생 가능성 있음.

### 테스트/검증
- 기존 테스트 케이스 업데이트 및 새로운 API 함수와 UI 컴포넌트에 대한 단위 및 통합 테스트 필요.
- 방명록 및 판매 수량 조정 기능의 정상 작동 여부 검증.
- Axios 요청 및 응답 처리 로직에 대한 테스트 수행.

### 후속 조치
- 변경된 API 경로 및 새로운 기능에 대한 문서화 필요.
- 사용자 피드백 수집 및 UI/UX 개선 사항 반영 계획.
- 인증 관련 로직의 안정성을 높이기 위한 추가 테스트 케이스 작성.
